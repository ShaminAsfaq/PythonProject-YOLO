# Ignore entire datasets folder (and all its subfolders/files)
datasets/

# Ignore entire results folder (and all its subfolders/files)
results/
video_results/

# Ignore all video files anywhere in the project
**/*.{mp4,avi,mov,wmv,flv,mkv,webm,mpeg,mpg,m4v,3gp,3g2}
*.{mp4,avi,mov,wmv,flv,mkv,webm,mpeg,mpg,m4v,3gp,3g2}
