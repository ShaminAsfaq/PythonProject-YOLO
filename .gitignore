# Ignore all images in datasets/ and its subfolders
datasets/**/*.{png,jpg,jpeg,gif,bmp,tiff,webp,svg}

# Ignore all video files in the entire project, including subfolders
**/*.{mp4,avi,mov,wmv,flv,mkv,webm,mpeg,mpg,m4v,3gp,3g2}
